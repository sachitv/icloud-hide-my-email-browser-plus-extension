# Copilot Instructions

- When generating commit messages, always use the Conventional Commits format: `type(scope): subject` (the scope is optional).
- When suggesting pull request titles, also use the Conventional Commits format: `type(scope): subject` (the scope is optional).
